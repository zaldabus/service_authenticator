require 'spec_helper'

describe ServiceAuthenticator do
  it 'has a version number' do
    expect(ServiceAuthenticator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
